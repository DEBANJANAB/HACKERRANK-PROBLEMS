#include<stdio.h>
#include<stdlib.h>
int main() {
	int q,x,y,z;
		
	scanf("%d",&q);
	while(q>0) {
	scanf("%d %d %d", &x, &y, &z);
		
		if(abs(z-x) < abs(z-y)){
			printf("Cat A %d  %d",abs(z-x),abs(z-y));
		}
#include<stdio.h>
#include<stdlib.h>
int main() {
	int q;
    scanf("%d",&q);
    for(int i=0;i<q;i++){
        int x, y ,z;
	scanf("%d %d %d", &x, &y, &z);
        
		if(abs(x-z) == abs(y-z)){
			printf("Mouse C\n");
		}
		else if(abs(y-z) < abs(x-z)){
			printf("Cat B\n");
		}
		else {
			printf("Cat A\n");
		}
    }       
		return 0;
} 
		if(abs(z-y) < abs (-x){
			printf("Cat B");
		}
		if(z-x == z-y){
			printf("Mouse C");
		}
q--;	}
		return 0;
	} 
